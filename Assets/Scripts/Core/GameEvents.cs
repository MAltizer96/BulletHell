using System;

public static class GameEvents
{
    //public static event Action OnPlayerRestarts;
    //public static event Action<iDamageable> OnPlayerDied; // add this

    ////public static void PlayerRestarts() => OnPlayerRestarts?.Invoke();
    //public static void PlayerDied(iDamageable player) => OnPlayerDied?.Invoke(player);
}